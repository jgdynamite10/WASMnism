# ML Models

> **This directory is only relevant on the `ml-inference` branch (Tier 2).**
>
> The `rules-only` branch does not use ML inference. Model files are not
> needed for building or deploying Tier 1 platforms (Akamai Functions,
> Fastly Compute, Cloudflare Workers).

See the `ml-inference` branch for:
- Model download instructions
- MiniLMv2 architecture and fine-tuning details
- Conversion pipeline (PyTorch → ONNX → Tract NNEF)
- SHA-256 checksums
